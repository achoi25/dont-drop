#!/bin/sh
set -e

echo "--- backend ---"
cd backend
uv run ruff format .
uv run ruff check --fix .
uv run ty check
cd ..

if [ -f frontend/package.json ]; then
  echo "--- frontend ---"
  cd frontend
  npm run format
  npm run check
  cd ..
fi
